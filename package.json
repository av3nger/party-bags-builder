{
  "name": "party-bag-builder",
  "version": "1.0.0",
  "main": "party-bag-builder.php",
  "scripts": {
    "build": "wp-scripts build --webpack-src-dir=includes/blocks --experimental-modules --blocks-manifest",
    "start": "wp-scripts start --webpack-src-dir=includes/blocks --experimental-modules --blocks-manifest",
    "lint:js": "wp-scripts lint-js 'includes/blocks/**/*.js'",
    "format": "wp-scripts format 'includes/blocks/**/*.js'"
  },
  "keywords": ["plugin", "wordperss"],
  "author": "vCore Digital",
  "license": "MIT",
  "description": "Party bag builder WordPress plugin",
  "devDependencies": {
    "@wordpress/scripts": "^30.25.0"
  }
}
